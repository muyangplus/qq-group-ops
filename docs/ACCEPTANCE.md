# 真实环境验收清单

本文对应 [ROADMAP](ROADMAP.md) Phase 1 的退出条件，用于在**真实 QQ 群**中逐项验收。每个验收项都给出操作、预期结果与失败排查方向。

> 建议在一个测试群 + 一个测试机器人上执行，避免影响正式群。
> 执行前请确认 `.env` 已填写 `QQ_BOT_APP_ID`、`QQ_BOT_CLIENT_SECRET`、`ADMIN_USER_IDS`。

## 0. 准备

```bash
pnpm install
pnpm class:index          # 可选：配置班级+姓名入群规则时需要
cp .env.example .env      # 填写 AppID / Secret / ADMIN_USER_IDS
pnpm typecheck && pnpm test && pnpm build
pnpm dev
```

`pnpm test` 中的 `test/acceptance.test.ts` 已用真实 SQLite 文件 + 官方 API 测试替身自动跑通
B/C/D/G 组的核心链路（审批闭环、关键词警告/处罚与审计、班级+姓名入群规则、按群隔离、重启恢复、动态 `/help`），
因此人工验收只需聚焦**依赖真实 QQ 平台**的部分（真的进群、真的禁言/踢人、真实事件推送）。

启动后应看到（`logs/qq-group-ops.log` 或控制台）：

- `bot cache status { tokenCached, gatewayUrlCached, cacheFile }`
- `gateway hello { heartbeatIntervalMs }`
- `gateway ready: bot authenticated`

| # | 验收项 | 操作 | 预期结果 | 失败排查 |
|---|---|---|---|---|
| A1 | 机器人上线 | `pnpm dev` | 日志出现 `gateway ready`，`/test` 有响应 | 检查 AppID/Secret；是否被限流（`400 频率限制`） |
| A2 | 首次拉起缓存 | 首次启动后看日志 | 出现 `access token acquired` 与 `gateway url received` | 反复出现说明缓存文件不可写（`QQ_BOT_CACHE_FILE`） |
| A3 | 重启复用缓存 | 重启进程 | 出现 `gateway url reused from cache`、`access token reused from cache`，且**不再**请求 `/gateway` | 若仍请求，检查缓存文件是否被删或 AppID 变化 |
| A4 | 限流不硬打 | 反复快速重启 5 次 | 不出现 `/gateway` 400；必要时出现 `cooling down` 日志 | 仍报 400 说明缓存未生效 |

## 1. 绑定与权限

| # | 验收项 | 操作 | 预期结果 |
|---|---|---|---|
| B1 | 用户绑定 | 群内或私信 `/bind qq <QQ号>` | `已绑定：userId ... ↔ QQ ...` |
| B2 | 查询自己的权限 | `/myperm` | 显示权限等级与各项能力布尔值 |
| B3 | 动态帮助 | `/help` | 只出现当前用户有权限执行的指令；未绑定用户只看到 `/help`、`/bind` |
| B3b | 指令主题帮助 | 管理员执行 `/help rules`，普通成员也执行一次 | 管理员看到完整字段说明 + 该群当前生效值；普通成员只看到「权限不足 + 所需权限」，不展示 `/rules set` 细节 |
| B4 | 用 QQ号执行命令 | 超管 `/whois <QQ号>` | 返回对应 userId |
| B5 | 群号绑定 | 群管理员 `/bind group <群号>` | `已绑定：group_openid ... ↔ 群号 ...` |
| B6 | 强制绑定 | 未绑定用户在群里发 `/myperm` | 返回 `请先绑定 QQ 号：/bind qq <QQ号>` |
| B7 | 权限持久化 | 超管 `/perm grant mod <QQ号>` 后重启进程 | `/myperm` 仍是 moderator，权限未回退 |
| B8 | 本群超管只在本群生效 | 超管 `/perm grant gsuper <QQ号>`（在群 g1 内），再让该用户去另一个群执行 `/approve` | 群 g1 内可审批/改规则；别的群或私信中报「权限不足」；`/perm`、`/rules all` 始终被拒 |
| B9 | 全局超管不被本群超管顶掉 | 只配置本群超管后重启进程 | 日志出现 `seeding super admins from configuration`，`ADMIN_USER_IDS` 仍是全局超管 |
| B10 | 超管私信看帮助 | 全局超管私信 `/help rules`、`/help notify` | 正常返回详细帮助，不再出现「权限不足」 |
| B11 | 超管私信看全局规则 | 全局超管私信发 `/rules`（不带群号） | 显示全局默认规则（等价 `/rules all`） |
| B12 | 只显示解析号 | 绑定 QQ号/群号后执行 `/pending`、`/status`、`/perm list`、`/audit`，并触发一次入群推送 | 输出只出现 QQ号/群号，不出现 `userId`/`group_openid`；未绑定的申请人仍显示内部 id；`/whois` 仍同时显示两边 |
| B13 | 未绑定走短码 | 让未绑定 QQ号的用户申请入群，再看 `/pending` 与推送卡片 | 申请人显示 `#XXXXXX` 短码（不是 openid），申请单号也是 `#XXXXXX`，正文没有长 id |
| B14 | 短码可执行、可还原 | 用 `/pending` 里的短码执行 `/approve #XXXXXX`；再让超管执行 `/whois #XXXXXX` | 审批成功；`/whois` 显示短码对应的类型与真实系统 id（普通用户执行 `/whois` 被拒） |

## 2. 入群审批闭环（Phase 1 关键退出条件）

| # | 验收项 | 操作 | 预期结果 |
|---|---|---|---|
| C1 | 收到申请 | 用小号申请入群（或先 `/sync` 拉取） | `/pending` 能看到申请 ID 与申请人 |
| C2 | 官方同步 | `/sync` | 输出 `已同步官方待审批申请，当前待审批 N 条`；30 秒内重复执行提示冷却 |
| C3 | 通过 | `/approve <申请ID>` | 回复 `已通过入群申请 ...`，**且该用户真的进入群** |
| C4 | 拒绝 | `/reject <申请ID> 资料不完整` | 回复 `已拒绝...`，用户未进群，`/audit` 里 reason 为「资料不完整」 |
| C5 | 官方失败不误报 | 权限不足/接口报错时执行 `/approve` | 返回 `审批失败：...`，`/pending` 里申请仍是待审批 |
| C6 | 自动通过 | `/rules set autoApprove on` 后再有申请 | 无需人工操作即通过（日志 `auto approved join request`） |
| C7 | 班级+姓名规则 | 先 `pnpm class:index`，再 `/rules set joinRequireClass on`、`/rules set joinRequireName on`、`/rules set joinDecision approve_on_match`、`/rules set joinReviewOpinion on`；用正确回答与错误回答各申请一次 | 正确 → 自动通过；错误 → 保持待审批，`/pending` 显示识别到的班级/姓名与「建议：人工审核」 |
| C8 | 规则不误放行 | 删除/改名 `data/class-index.json`，或把 `joinAnswerPattern` 设成无效正则（应被拒绝保存），再触发一次申请 | 规则无法判定时一律转人工，日志有 `configIssue`，绝不会自动通过 |
| C9 | 决策模式 | 分别试 `reject_on_match` 与 `reject_on_mismatch` | 命中/未命中按表格语义自动拒绝，且 `/audit` 里 reason 为截断后的审核意见 |
| C10 | 订阅推送 | 群管理员在群里 `/notify on` | 回复「已开启」；`/notify` 显示「群 654321：已开启」；重启后 `/notify` 仍是已开启 |
| C11 | 收到卡片 | 用小号再申请一次入群 | 私聊收到 Markdown 卡片：群号、申请人、回答、审核意见，底部第一行「同意 / 拒绝」、第二行两个红色预设「拒绝：回答错误」「拒绝：班级姓名」 |
| C12 | 按钮审批 | 点「同意」并在二次确认里确认 | 自动发送 `/approve <group> <申请ID>`，申请通过；`/audit` 出现 `approve_join_request` |
| C12b | 预设拒因 | 点「拒绝：回答错误」并确认 | 自动发送 `/reject <group> <申请ID> 请正确回答问题。`，申请人收到的拒绝理由即为该文案 |
| C12c | 问答式入群验证 | 群设置为「管理员设置问题」（`admin_review_qa`）后申请一次 | 卡片「回答」显示申请人填写的答案、「入群问题」显示管理员设置的问题；班级+姓名规则能识别到 |
| C13 | 按钮未开通时降级 | 若应用没有自定义按钮白名单 | 仍能收到纯 Markdown（或纯文本）推送，内容带完整指令；日志出现键盘降级告警，审批流程不受影响 |
| C14 | 主动消息失败可见 | 在 QQ 客户端关闭「允许主动发送」后再申请一次 | 推送失败只记日志与投递状态，申请仍在 `/pending`；`/notify test` 返回失败提示 |
| C15 | 自动处理通知开关 | `/rules set joinDecision approve_on_match` + `/rules set notifyAutoApproved on`，再用正确回答申请一次；然后设 `notifyAutoApproved off` 再申请一次 | 开启时收到只读卡片「已自动通过（按入群规则）」且没有审批按钮；关闭时完全静默（只写审计） |

> C3 依赖官方 `approval_join_request` 接口与机器人权限；若返回 `11253` 或权限错误，请确认机器人在该群的管理员身份与开放平台权限。

## 3. 关键词审核

| # | 验收项 | 操作 | 预期结果 |
|---|---|---|---|
| D1 | 配置关键词 | 群管理员 `/rules set keywords 测试违禁词` | 返回 `已更新群规则`，`/rules` 里能看到该关键词 |
| D2 | 命中警告 | 群里发送含关键词的消息 | 机器人发出该群警告文案（`/rules set warning ...` 可改） |
| D3 | 审计留痕 | `/audit` | 出现 `moderation:warn` 记录，reason 为 `命中关键词：...` |
| D4 | 按群隔离 | 在另一个群发同样内容 | 不触发（除非该群也配置了关键词） |
| D5 | 立即生效 | 追加一个关键词后立刻测试 | 无需重启即生效 |
| D6 | 重启保留 | 重启进程后 `/rules` | 关键词仍在 |
| D7 | 关闭过滤 | `/rules set wordFilter off` 后再发关键词 | 不再触发 |
| D8 | 命中撤回 | `/rules set keywordRecall on` 后发含关键词的消息 | 消息被撤回，且仍发出警告；`/audit` 的 detail 含 `recall` |
| D9 | 命中处罚 | `/rules set keywordPunish mute`（或 `kick` / `kick_blacklist`）后再发 | 该成员被禁言/移出/移出并拉黑；日志里能看到动作成功或 `*_failed`，全部失败时 `/audit` 状态为 `pending` 且警告仍会发出 |

## 4. 非 @ 指令识别

| # | 验收项 | 操作 | 预期结果 |
|---|---|---|---|
| E1 | 开启全量消息 | 群管理员在机器人资料页开启「接收所有消息」 | 日志出现 `group message reception enabled` |
| E2 | 状态可见 | `/status` | `全量消息模式：all` |
| E3 | 非 @ 指令 | 群里直接发 `/myperm`（不 @ 机器人） | 机器人正常回复 |
| E4 | 未开启时行为 | 关闭「接收所有消息」 | `/status` 显示 `at_only`，非 @ 指令无响应（属预期） |

## 5. 禁言 / 踢人（可选，取决于开放平台权限）

| # | 验收项 | 操作 | 预期结果 |
|---|---|---|---|
| F1 | 禁言 | `/rules set keywordPunish mute` 后触发关键词 | 该成员被禁言（时长取 `muteDuration`），`/audit` 有记录 |
| F2 | 解除禁言 | 时长为 0 | 立即解除 |
| F3 | 踢人 | `/rules set keywordPunish kick`（或 `kick_blacklist`）后触发关键词 | 成功；返回 `11253` 说明应用未在白名单，需要向平台申请 |
| F4 | 纯拉黑 | 目标不在群时调用黑名单接口（`updateMemberBlacklist`） | 成功；目标是群成员时该接口会报错，属预期 |

> 关键词命中的默认动作是「警告」；撤回与处罚由 `keywordRecall` / `keywordPunish` 配置（见 README「关键词处罚与入群审核规则」）。

## 6. 持久化与数据保留

| # | 验收项 | 操作 | 预期结果 |
|---|---|---|---|
| G1 | SQLite 默认 | 不配置 `DATABASE_URL` 启动 | `databaseDriver: sqlite`，生成 `data/qq-group-ops.db` |
| G2 | 停机不丢数据 | 改配置/绑定后正常 Ctrl+C 退出再启动 | 数据仍在 |
| G3 | PostgreSQL 可切换 | `.env` 设 `DATABASE_URL=postgres://...` + `pnpm db:up` | `databaseDriver: postgres`，功能一致 |
| G4 | 内存模式 | `DATABASE_URL=memory` | 启动告警，重启后数据清空（预期） |
| G5 | 保留清理 | 日志中查看 `retention cleanup finished` | 启动时执行一次，无过期数据时为 0 |
| G6 | 规则全部入库 | 逐项执行 `/rules set`（含 `all` 与只改扩展字段的情况）后重启进程 | 每个字段都保持修改后的值：单群规则、全局规则、仅扩展字段的群覆盖都不丢 |

## 7. 个人资料与活动

| # | 验收项 | 操作 | 预期结果 |
|---|---|---|---|
| H1 | 关键词豁免 | 审核员（moderator）在群里发含关键词的消息 | 不警告、不撤回、不处罚；日志出现 `moderation exempt`；普通成员发同样内容仍被警告 |
| H2 | 填写资料 | `/profile set id 22123456789`、`/profile set name 小明`、`/profile set class 材化2211` | 学号/姓名/班级保存成功；学院由班级库自动带出；`/profile` 显示全部字段与年级 |
| H3 | 资料校验 | `/profile set id 12345`、`/profile set class 不存在的班级`、`/profile set year 2022` | 前两项分别返回「11 位」「不在班级库」的失败提示；年级四位年份返回「只写两位」（用 `22` 才成功），三种情况都不写入 |
| H4 | 发布活动 | 群管理员 `/activity create 迎新晚会` → `/activity set #短码 capacity 2`、`link 报名入口=https://example.com/signup`、`allowYears 22` → `/activity open #短码` | 群里收到 Markdown 卡片（含链接、限制、报名人数），底部有报名/取消/详情/名单按钮 |
| H5 | 报名 | 资料完整的 22 级同学点「报名」或 `/activity join #短码` | 报名成功并显示序号/人数；重复报名提示「已经报名」 |
| H6 | 限制生效 | 23 级同学报名（`allowYears 22`） | 拒绝并提示「仅限 … 级报名」；黑名单学院优先拒绝 |
| H7 | 资料不全 | 未填 `/profile` 的同学报名 | 提示先补全「姓名 + 学号 + 班级」 |
| H8 | 名单与权限 | 群管理员 `/activity signups #短码`；普通成员执行同样指令 | 管理员看到姓名/学号/班级/学院；普通成员提示权限不足 |
| H9 | 重启保留 | 报名后重启进程，再 `/activity info #短码`、`/profile` | 活动（短码/链接/限制）、报名记录与个人资料都还在 |
| H10 | 候补 / 冻结名额 | `capacity 1` 的开放活动：两人报名，让第一位取消报名 | 第二位进候补；管理卡显示「待释放名额 1」；点「释放名额」后第二位被递补（私信通知），管理卡恢复 |

## 8. 交互菜单（`/menu`）

| # | 验收项 | 操作 | 预期结果 |
|---|---|---|---|
| J1 | 主菜单 | 群内或私信发 `/menu` | 收到 Markdown 卡片：显示当前权限与群号，入口按权限显隐 |
| J2 | 空 @机器人 | 群里只 @机器人、不带内容 | 直接收到主菜单（不是审核报错） |
| J3 | 权限过滤 | 普通成员发 `/menu admin`、`/menu super` | 均提示权限不足；成员的主菜单里不出现这些入口 |
| J4 | 子菜单与按钮 | 点「待审批列表」/「群规则」等按钮 | 等价于发送 `/pending`、`/rules`，输出与手输一致 |
| J5 | 未知指令 | 发 `/not-a-command` | 先提示未知指令，并附上菜单入口按钮 |
| J6 | 按钮未开通 | 在未开通自定义按钮的机器人上发 `/menu` | 自动降级为 Markdown / 纯文本，正文列出同样的指令，功能不丢 |
| J7 | 私信首次推送 | 用一个全新用户首次私信机器人 | 收到一次主菜单；再发消息不再重复推送（正式启动入库，重启也不重复） |
| J8 | dev 内存态 | `pnpm dev` 启动后首次私信，重启进程再私信 | 重启后会再收到一次（`MENU_FIRST_PUSH` 未设置时 dev 默认 memory） |
| J9 | 回调翻页 | 超管发 `/testmenu`，点「下一页」 | 客户端 loading 结束后收到第 2 页卡片；日志出现 `INTERACTION_CREATE`、`respondInteraction ok`、`page_2` |
| J10 | 回调兜底 | 点按钮后看日志与聊天 | 发送失败时机器人仍主动发出目标页（`active_fallback`）；键盘不会被误判禁用（后续页仍有按钮） |
| J11 | 非超管点击 | 让普通成员点卡片上的按钮 | 回包并提示「仅全局超级管理员可以翻页」，不翻页 |
| J12 | 全量卡片 | 逐条执行 `/myperm`、`/bind`、`/whois`、`/profile`、`/perm list`、`/activity` | 每条都返回卡片（不是纯文本）并带按钮；非 help 卡片正文不再罗列手动等价指令 |
| J13 | 菜单覆盖全部指令 | 走一遍 `/menu` → 系统 / 管理 / 超管 / 活动 / 审核操作 / 活动运营 | 每级都是卡片；需要参数的指令在正文给用法，不出现点不动的死按钮 |
| J14 | 导航是回调 | 点菜单里的「管理菜单」「待审批」「群规则」等 | 点击即出现对应卡片（不需要再发消息）；群内旧卡片保留 |
| J15 | 回调动作有反馈 + 操作人 | 点 `/rules` 的开关/枚举、`/pending` 的「通过」、`/notify` 的订阅开关 | 卡片正文**首行单独一行 @ 操作人**（`<@!openid>`，私聊省略），其后才是「结果：…」，且状态真的变了 |
| J16 | 排版约束 | 观察任意卡片 | 每行按钮文字合计 ≤12 字、单个按钮 ≤10 字、整盘 ≤5 行；过挤的已拆成子卡 |
| J17 | 列表分页 | `/pending`、`/audit`、`/activity` 列表 | 列表每页条数固定（§卡片规范 v2 目标 5 条，键盘受限的按规范例外表）；翻页全部是回调按钮，卡片正文**不写** `+页码` 指令（用法只进 `/help`）；手输 `+页码` 仍然有效 |
| J18 | 申请队列自动过期 | 把 `JOIN_REQUEST_TTL_DAYS` 设为 1，等超过 24 小时后看 `/pending` 与 `/audit` | 过期申请不再出现在 `/pending`；`/audit` 有 `expire_join_request`（状态 `expired`）；`/whois <#短码>` 仍能看到详情 |
| J19 | 官方对账 | 在官方侧处理掉一个申请后再 `/sync`（或等官方列表不再返回它） | 超过 1 小时的本地待审批被标记为过期，`/pending` 不再显示 |
| J20 | `/whois` 详情 | `/whois <#申请短码>`（群内） | 群里**完全静默**（连提示都不回）；私信里收到完整详情（群、申请人、理由、状态、申请/处理时间与处理人） |
| J21 | 智能 `/profile set` | 依次试 `/profile set 材化2211 张三 22123456789`、`/profile set 张三-22123456789-材化2211`、`/profile set 材化2211张三22123456789`、`/profile set 班级=材化2211 姓名=张三 学号=22123456789` | 四种写法都写入成功并回显「识别到：…」；学院/年级由班级自动带出，年级显示「22 级」 |
| J22 | 智能识别歧义不猜 | `/profile set 材化2211 环工2414 张三`、`/profile set 材化2211 张三 abc`、`/profile set 班级=材化9999 姓名=张三` | 三种都**不写入**任何字段；前两种列出识别结果，第三种提示班级不在班级库中 |
| J23 | `/whois profile` | 超管在群里发 `/whois profile <QQ号>`、`/whois profile #用户短码`、`/whois profile <未填资料的QQ号>` | 群里**完全静默**，私信里给出 userId / QQ号 / 短码 + 姓名 / 学号 / 班级 / 学院；未填资料显示「尚未填写」；群管理员执行提示权限不足 |
| J24 | 别名表（超管） | 超管发 `/alias set 环工2214 环境类2214`、`/alias`、`/alias del 环工2214` | 保存/列表/删除都成功；`/alias set x 不存在的班级` 报「不在班级库」；群管理员执行 `/alias` 提示权限不足 |
| J25 | 别名参与识别 | 建别名后，用别名发 `/profile set 环工2214 张三 22123456789`，并让新成员用别名回答入群问题 | profile 里落库的是规范名 `环境类2214` 且学院自动带出；入群审核按别名也能命中班级（`approve_on_match` 自动通过） |
| J26 | 别名持久化 | 建别名后重启进程，再 `/alias` | 别名仍在（`class_aliases` 表），无需重新维护 |
| J27 | `/whois` 只走私信 | 超管在群里发 `/whois`、`/whois <#申请短码>`、`/whois profile <QQ号>`；再故意关闭私信（或在从没私聊过的账号上操作）重试 | 群里**完全静默**（连提示都不回，且不含任何查询内容）；私信里收到结果；私信失败时群里只回一条「请先私聊机器人再试」，仍然不出现结果 |
| J28 | 群内 @ 查询 | 群里发 `/whois <@某成员>`、`/whois profile <@某成员>`；再发 `/whois @昵称` | 前两条把该成员的映射/资料私信给操作人；`@昵称` 提示「无法从 @昵称 反查用户」并给出替代写法 |
| J31 | @ 渲染对照 | 超管在群里发 `/testat` 看三条测试消息；再发 `/testat all` 看 5 条 @全体候选 | 汇总卡报告 8 条已发送；记录「卡片内 @ 是否生效」（本机已实测：2、3 生效；1 纯文本无效）与「@全体候选里有没有真能提醒全群的写法」；发送失败时汇总卡列出失败条目 |
| J29 | 短码全大写 | 观察 `/pending`、`/audit`、活动卡片上的短码；用旧的小写短码尝试 `/approve #xxxxxx` | 展示的短码只含数字与大写字母；旧小写短码在重启后失效（需重新获取）；手输 `#小写` 仍能解析当前有效短码 |
| J30 | 年级只两位 | `/profile set year 2022`、`/profile set year 22`、`/profile set 班级=材化2211`、重启后再 `/profile` | 四条依次：报错「只写两位」；成功；班级带出的年级是两位；重启后 `/profile` 仍是「22 级」 |
| J32 | 活动列表分组 | 群里 `/activity`（至少有一个草稿 + 一个已开放的活动） | 卡片按「报名中 / 草稿 / 已结束」分组；每个活动一行「详情 / 报名 / 管理（仅管理者）/ 订阅」，全部是回调；`/activity list +2` 能翻页 |
| J33 | 配置卡 | 群管理员 `/activity create 迎新晚会`；再点列表里的「管理」/配置卡上的「配置」 | 创建后直接返回**配置卡**：短码/标题/状态/名额/限制/截止/递补/@全体提示/报名通知；按钮含名额 `10 20 50 不限` + 自定义（指令按钮预填）、学院/年级限制、截止（不限/自定义）、`递补 自动/手动`、`报名通知 开/关`、`提醒@全体 开/关`、预览卡片 / 开放报名 / 取消活动；点回调会**刷新同一张配置卡**并在正文顶部给出结果行 |
| J34 | 报名落点（群内） | 资料完整的成员在群里点成员卡的「报名」并确认二次确认 | §B4 覆盖：群里**一条消息都不发**（见 J45）；结果只私信给本人 |
| J35 | 报名落点（私聊） | 同一个人在私信里点活动卡片的「报名」 | 私聊回执可以包含姓名/学号/序号/人数（隐私字段只在私聊出现） |
| J36 | 报名失败只私信 | 直接点报名但资料不全 / 年级不符合 `allowYears` / 已报名 | §B4 覆盖：群里静默（见 J46）；**具体原因**（补全资料 / 仅限 … 级 / 已报名 / 已截止）只在私信里出现 |
| J37 | 候补与冻结名额 | 名额满后再报名；随后让已报名者「取消报名」 | 报名者私信收到「已进入候补 · 第 N 位」（群内静默）；取消后管理卡出现「待释放名额 M」，点「释放名额」后候补第一位被**私信**通知「你已递补成功（当前 Y/Z）」，**不往群里发**；`waitlistPromotion auto` 时取消即自动递补 |
| J38 | 名单分页与脱敏 | 群管理员点成员卡/管理卡的「报名名单」；再点「完整信息」翻页 | 默认每页 5 人（§卡片规范 v2），只显示「序号 姓名（班级）备注」，**没有学号/学院**；点「完整信息」才显示学号/学院；「上一页/下一页/完整信息」都是回调按钮，卡片里不写指令；候补区单独列出（最多 10 个 + 「还有 N 人」）；普通成员点同一按钮得到「权限不足」卡 |
| J39 | 订阅开关与发布推送 | 成员 A 点成员卡/详情卡的「订阅」；管理员随后发布新活动 | 订阅卡显示「订阅 开/关」并写入 `activity_subscriptions`（重启后仍为开）；发布后**只有订阅者**在私信收到活动卡，群里不发订阅通知；再次发布同一活动不会重复推送（去重） |
| J40 | 发布回执的 @全体提示 / 越权回调 | 管理员 `/activity set #短码 mentionAll on` 后 `/activity open #短码`；再让普通成员点卡片上的「管理」「释放名额」 | 操作者私信回执含「**机器人无法 @全体成员，如需通知全群请手动 @ 一条**」且群里那张卡不会出现 `@everyone` / `<@!all>`；普通成员调用管理类回调只得到「权限不足」卡，活动状态不变 |
| J41 | 统计图片 | 群管理员在管理卡点「统计图片」 | 群里收到一张统计图（宽度 720、高度自适应）：标题、`报名 X/Y`、`候补 N`、`待释放名额 M`、`截止`、学院分布与年级分布（横向条形 + 人数）；操作者另收到一张「已发送统计图片」卡 |
| J42 | 统计图片降级 | 在没有系统中文字体、也下载不到字体的环境（或未装 `@napi-rs/canvas`）点「统计图片」 | **启动不受影响**；回调返回**文字统计卡**（报名 / 候补 / 待释放 / 学院分布 / 年级分布 / 截止），正文底部说明「统计图片不可用」；日志只有 warn，没有异常抛出 |
| J43 | 统计图字体缓存 | 首次点击后看 `data/fonts/`，再重启后点一次 | 首次：优先用系统字体（Windows 雅黑 / Linux Noto CJK），系统没有才从 `ACTIVITY_STATS_FONT_URL` 下载并缓存到 `data/fonts/`；重启后复用缓存（不再下载）；`git status` 里**看不到**缓存文件（`data/` 已 gitignore，不随包提交） |
| J44 | CSV 导出 | 群管理员在报名名单卡点「导出 CSV」 | CSV 私信给**操作者本人**（群里不回执内容），列固定为 `序号,姓名,学号,班级,学院,备注,候补`，候补行最后一列为 `候补`；超过单条消息长度上限时不硬塞，改为私信提示用 `/export #短码` 拿完整文件；普通成员调用同回调得到「权限不足」卡 |
| J45 | 群内报名静默（§B4） | 资料完整的成员在群里点成员卡的「报名」并确认二次确认；再手输一次 `/activity join #短码` | 群里**一条消息都不发**（连「原因已私信」都没有）；本人私信收到「报名成功 · 当前 X/Y」+ 姓名/学号/班级；重复报名时群里同样静默，私信收到「你已经报名过该活动了」 |
| J46 | 群内取消静默与私信兜底（§B4） | 成员在群里点「取消报名」；随后换一个**从没私聊过机器人**的账号点「报名」 | 取消：群里静默，私信回执（manual 模式含「待释放名额 +1」）；私信发不出去时群里只回 `@申请人 私信发送失败，请先私聊机器人再试`，**不含任何结果字段**；私聊里 `/activity join\|quit` 仍原地回复 |
| J47 | 多群绑定与发布（§B4） | 群管理员 `/activity bind #短码 <另一个群号>`，再 `/activity open #短码` | 配置卡正文出现「绑定群」行与「绑定群」入口；发布后**两个绑定群各收到一张成员卡**；操作者私信回执列出「成功 2 个 / 失败 0 个」；点「解绑」或 `/activity unbind` 后重发只发到剩下的群 |
| J48 | 满员广播（§B4） | 活动绑定两个群、`capacity` 设为 2；让第 1、2 位报名（第 2 位恰好满员），再让第 3 位报名 | 第 2 位报名后**两个绑定群各收到一次**「活动已满 2/2」卡（正文含「后续报名将自动进入候补队列（当前候补 N 人）」）；第 3 位报名进候补，**不再重复广播**；去重行是 `(活动, group:<群ID>, full)` |
| J49 | 规则概览与当前状态（§C） | 群管理员发 `/rules`，先点「开关设置」子卡，再点「过滤 开」按钮 | 概览卡正文显示「**本群覆盖**：…（其余继承全局）」或「全部继承全局」，入口 5 个子卡（开关设置 / 入群审核 / 违规处理 / 关键词 / 更多设置）；开关按钮标签显示**当前状态**（`过滤 开`），点击后切换并回到同一张子卡（标签变 `过滤 关`），正文对应行变「本群覆盖」 |
| J50 | 字段级恢复本页继承（§C） | 在同群分别用 `/rules set keywords 广告`、`/rules set wordFilter off`、`/rules set keywordRecall on` 设置覆盖，再点「开关设置」子卡的「恢复本页继承」并确认 | 只清本页字段（`wordFilter` / `joinAudit` / `keywordRecall` / `export`），这些字段回落继承；**关键词覆盖不受影响**；概览卡的「本群覆盖」也随之减少 |
| J51 | 恢复全部继承（§C） | 在概览卡点「恢复全部继承」并确认 | 本群全部字段级覆盖被清空，所有字段回落到全局默认；概览卡显示「全部继承全局」；重启后仍为继承 |
| J52 | 关键词逐条增删与分页（§C） | `/rules add keyword 广告`、`/rules add keyword 刷屏`；再点「关键词」子卡里的「删 …」；对重复词发 `/rules add keyword 广告`、对不存在词发 `/rules del keyword 不存在` | 加词 trim / 去重（重复报「已存在」）/ 单条 ≤50 字（超长报错）；子卡每页 3 条、每条一个「删」按钮，删除后回到本页；`/rules del keyword 不存在` 明确报错；「清空」二次确认后清空 |
| J53 | 学院 / 年级点选（§C） | 点「名单筛选」子卡，切到「白名单」后点一个学院、点一个年级；再切到「黑名单」点选；重启后查看 | 学院按钮来自 `MemberRoster.listColleges()`（每页 4 个 + 翻页，`●` 标记已选），年级来自 `PROFILE_ENTRY_YEARS`（22-26）；选择落到 `allowColleges/allowYears` 或 `denyColleges/denyYears` 覆盖，重启后仍在；普通成员调用同一回调得到「权限不足」卡 |
| J54 | 全局规则同构与覆盖率总览（§C） | 超管发 `/rules all`，进入各子卡；再发 `/rules overrides` | 全局规则卡与群规则**同一套子卡结构**（目标 `DEFAULT_GROUP_ID`），正文标明「只影响未单独覆盖的群」；「覆盖率总览」按 `listOverrideSummaries()` 列出每个群的覆盖字段（每页 5 群，§卡片规范 v2）；非超管调用只得到「权限不足」卡 |
| J55 | 全局清字段回落种子默认（§C） | 超管先 `/rules set all warning 全局文案`、`/rules set all autoApprove on`，再在全局「开关设置/更多设置」里「恢复本页继承」，然后重启 | 被清的全局字段回落到**种子默认**（内置默认/构造函数显式项）；没被清的全局覆盖（如 `autoApprove`）保持；未覆盖的群继续继承 |
| J56 | 规则降级路径与权限（§C） | 用 `/rules set keywords 广告,刷屏`、`/rules set all warning 全局文案` 手输；让普通成员点管理类回调 | `/rules set`（含 `all`）行为与权限完全保留：群管理员改本群、超管改全局；审核员只能查看；普通成员点「恢复继承 / 删关键词 / 名单点选」等回调只得到「权限不足」卡，规则不改变 |
| J57 | 处罚推送与卡片改处罚（§B7） | 审核员先 `/notify punish on`；再用小号在群里发一条命中关键词的消息（规则已设 `keywordPunish mute`） | 小号收到群内「**处罚通知**」卡（@ 当事人 + 处理动作 + 群规则文案，**不含命中的关键词/正则、不含原文**，带「我要申诉」按钮）；审核员私信收到「处罚通知」卡（记录短码 / 命中规则 / **原文**（未开启消息保留时为「（未保留原文）」）/ 执行动作）；点「禁言时长 → 1小时」后 `restrict_chat_setting` 生效且收到「已调整禁言时长」回执；点「解除处罚」后禁言解除；群内不发处罚通知 |
| J58 | 黑名单与入群审批最高优先级（§A5） | 审核员 `/blacklist add <小号>`（小号先在群里）；再由小号发起入群申请（或把它移出后重新申请） | 加入时小号被移出群、官方群拉黑调用成功（日志可见）；再次申请时**直接拒绝**（即使群配置是 `autoApprove on` 也不通过）；超管 `/blacklist add 全局 <小号>` 会把它从**所有绑定群**移出，且之后在任何绑定群申请都被拒绝；`/blacklist list 全局` 能看到条目；`/blacklist del` 后恢复正常 |
| J59 | 申诉闭环（§B8） | 被禁言的小号在群里点关键词卡上的「我要申诉」（只有它能看到可点），或私信发 `/appeal <#处罚短码> 误判` | 群里**完全没有**申诉相关消息；申诉人私信收到回执；订阅了处罚通知的审核员私信收到「申诉通知」卡；审核员点「通过申诉」后禁言解除、申诉人状态变为已通过；点「驳回申诉」则处罚不变；同一处罚重复申诉只更新理由、不重复推送 |

| J60 | 默认菜单与入口暴露（§F2） | 普通成员在群里发 `/menu`、群里空 @机器人、私信首次交互；管理员在群里发 `/menu` 与 `/menu 管理`；超管在群里发 `/menu 超管`，再在私信发 `/menu` | 默认入口一律是「常用菜单」（帮助 / 我的资料 / 我的权限 / 活动 / 申诉；未绑定显示绑定账号）；**群里任何情况下都不出现管理 / 超管按钮**；管理员群里 `/menu 管理` 能进管理菜单（越权仍提示权限不足）；超管群里 `/menu 超管` 只提示「请在私信中使用」；私信默认菜单里审核员及以上能看到「管理菜单」、全局超管还能看到「超管菜单」 |

| J61 | 群内回复首行 @ 发起人（§F1） | 群里用普通成员发 `/menu`、`/status`、`/rules`；管理员点一次群内回调按钮（如「刷新」）；再发 `/test`、`/testmenu`；私聊发 `/menu` | 群里的每一条回复卡片**首行都是 `<@!发起人>` 并真的 @ 到人**（含回调回复）；`/test*` 按现状不加 @；私聊不加 @；`/whois` 群内静默时没有任何消息，私信失败那条提示本身已 @ 发起人且不重复 |

| J62 | 菜单卡不含指令文本（§卡片规范 v2） | 依次发 `/menu`、`/menu 管理`、`/menu 超管`（私信）、`/menu 审核`、`/menu 运营`、`/menu 活动` | 每张菜单卡只有「标题 + 可选状态行 + 按钮」，正文里**不出现任何 `/xxx` 指令文本或「用法：」行**；点按钮能到达对应功能；用法只在 `/help`（及 `/help <主题>`）里出现 |
| J63 | 卡片无冗余指令行（§卡片规范 v2） | 抽查活动成员卡、报名未通过卡（私信 + 群内回执）、发布回执、`/binding`、`/whois`、`/status`、`/audit`、报名名单卡 | 卡片正文只有业务信息（谁 / 什么 / 数量 / 时间 / 短码）与操作按钮；**没有**「指令方式：」「用法：」「查看详情：/activity info …」「按钮不可用时可直接输入指令」这类行；「查看详情」是按钮不是文字 |
| J64 | 个人资料空态引导（§卡片规范 v2） | 用一个没有资料的账号发 `/profile` | 正文给出**一条可直接复制发送**的组合指令（`/profile set <学号> <班级> <姓名>`，顺序与分隔符随意），并有预填 `/profile set ` 的按钮；群内 `/profile` 静默 + 私信投递，失败才在群里 @ 提示 |
| J65 | 按钮覆盖全部可执行动作（§卡片规范 v2） | 逐张检查常用卡（菜单 / 规则子卡 / 处罚卡 / 申诉卡 / 活动卡 / 待审批 / 黑名单 / 通知卡） | 卡片要表达的每个动作都有对应按钮（导航、翻页、开关、动作）；按钮做不到的（如「自定义禁言时长」）才在底部保留一行等价指令；没有「按钮能做到却只写了指令」的遗漏 |
| J66 | `@全体` 段不被抢答（B3） | 群里依次发 `@全体成员`、`@everyone`、`@全体成员 大家好`、`@全体成员 /menu`、空 `@机器人` | 前两条机器人**完全不出声**（日志 `mention probe: skipped at-all mention`，`/audit` 无新记录）；第三条按普通发言走审核；第四条照常执行 `/menu`；第五条仍然回常用菜单（真机已确认官方表示：`<@all> ` / `@everyone`） |
| J67 | 处罚卡隐私口径 + 申诉降级 | 小号发一条命中关键词的消息（已开 `keywordPunish mute`）；审核员 `/notify punish on` 后看私信卡；让**被禁言的小号**在群里点「我要申诉」，再私聊机器人发 `/appeal #短码`；最后在私信引导卡上点「直接提交」 | 群内那张叫「**处罚通知**」：@当事人 + 处理 + 群规则文案（**没有命中的关键词/正则、没有原文**），footer 给出「私聊机器人发送 `/appeal #短码`」的兜底（被禁言时群内按钮点不动）；私信卡带「原文：」+ `> 引用段落`，**在标题下第一段且引用块后有空行**（后面的 `**群**：` 等字段没有被画进引用里；未开启消息保留时是「（未保留原文）」）；私信卡按钮**不带** `specifyUserIds`（否则客户端提示「无权限操作」），群内广播卡的按钮保留该限制；「直接提交」是回调，点了立即建单并回执，当事人卡片**不出现**审核员专属的「查看处罚」；私信发不出去时群里回一条「已按**无理由**提交申诉」、审核员照常收到申诉通知、申诉记录**已建单** |

## 9. 验收记录模板

```text
验收人：
验收时间：
机器人 AppID：
测试群 group_openid：
Node / pnpm 版本：
数据库：SQLite / PostgreSQL
提交版本：git rev-parse --short HEAD

A 接入网关：通过 / 未通过      备注：
B 绑定与权限：通过 / 未通过    备注：
C 入群审批闭环：通过 / 未通过  备注：
D 关键词审核：通过 / 未通过    备注：
E 非 @ 指令：通过 / 未通过     备注：
F 禁言/踢人：通过 / 未通过     备注：
G 持久化：通过 / 未通过        备注：
H 入群申请推送：通过 / 未通过  备注：
I 关键词豁免/资料/活动：通过 / 未通过  备注：
J 交互菜单 / 回调翻页 / 卡片标准：通过 / 未通过  备注：
K 活动卡片 / 回调 / 订阅（J32-J40）：通过 / 未通过  备注：
L 统计图片 / 富媒体上传 / CSV 导出（J41-J44）：通过 / 未通过  备注：
M 规则菜单重构 / 字段级继承（J49-J56）：通过 / 未通过  备注：
```

## 10. 失败排查速查

```bash
# 是否被限流（应只在首次启动出现 /gateway 请求）
rg '"url":"https://api.bot.qq.com/gateway"' logs/qq-group-ops.log
rg 'cooling down|rate limited' logs/qq-group-ops.log

# 网关连接与重连
rg 'gateway (hello|ready|reconnect scheduled|invalid session|resumed)' logs/qq-group-ops.log

# 事件是否到达
rg '"message":"dispatch"' logs/qq-group-ops.log

# 审批调用
rg 'approveJoinRequest|approved join request|auto approved' logs/qq-group-ops.log

# 审核命中
rg 'rule matched|moderation:' logs/qq-group-ops.log

# 入群申请推送（订阅、降级、失败）
rg 'notification (subscribed|delivered)|join request push|custom keyboard rejected|notification attempt failed' logs/qq-group-ops.log

# 持久化写入失败
rg 'persistence write failed' logs/qq-group-ops.log
```

常见问题：

- **非 @ 消息没反应**：群管理员未开启「接收所有消息」，用 `/status` 看 `全量消息模式`。
- **400 频率限制**：等待 1–2 分钟；确认 `QQ_BOT_CACHE_FILE` 生效，重启复用 token 与网关地址。
- **审批后没进群**：查看是否返回 `审批失败`；确认机器人在群内是管理员、开放平台侧有相应权限。
- **踢人失败 11253**：该接口仅白名单机器人可用，需要向平台申请。
- **收不到入群推送**：确认 `/notify` 已开启、你在这个群有审批权限、已 `/bind qq`；若是主动消息被关闭或按钮未开通，看日志里的降级/失败记录，`/notify test` 可自检。
- **点「统计图片」只回文字统计卡**：说明渲染被降级了，按日志排查——`activity stats render unavailable` 表示 `@napi-rs/canvas` 没装上或字体拿不到，`activity stats image delivery failed` 表示上传/发送失败（图片会被降级、文字统计照常给）。装依赖 `pnpm add @napi-rs/canvas`，或把 `ACTIVITY_STATS_FONT_URL` 指向可达的字体地址即可恢复。
- **统计图中文变方框**：系统字体命中的那份不含中文；换一份中文字体（放到 `SYSTEM_FONT_PATHS` 覆盖的路径）或让 `ACTIVITY_STATS_FONT_URL` 生效。
- **用 `/export` 而不是 CSV 私信**：名单超过单条消息长度上限时这是预期行为（避免被平台截断拿到半份名单）。
- **数据没保存**：确认 `DATABASE_URL` 不是 `memory`，并检查 `persistence write failed` 日志。
