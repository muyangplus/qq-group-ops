# Changelog

本项目遵循 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。

`0.1.0` 之后按「改动性质」定版本：**含用户可见新功能的版本按 MINOR 递增**（`0.2.0` → `0.9.0`），**只有修复与安全、行为修正的版本按 PATCH 递增**（`0.6.1`）；版本按时间倒序，日期格式 `YYYY-MM-DD`。
变更分类为 `新增` / `变更` / `修复` / `备注`；`备注` 用于记录能力边界与已知限制，不作为独立变更分类。

## [Unreleased]

### 修复

- **群里 `@全体成员` 会被机器人抢答菜单（§B3）**：真机确认官方表示是 `<@all> ` / `@everyone`，
  它们会被当作「@机器人」剥掉、剩下空内容，进而被当成「空 @机器人」回一张常用菜单。
  现在开头的 @全体 标记会被无条件剥离，且**整条只有 @全体、没有正文**时直接判为群广播丢弃
  （不回复、不送关键词审核、不写审计、不处罚，日志记 `mention probe: skipped at-all mention`）；
  `@全体成员 大家好`、`<@all> /menu` 这类带正文的不受影响（照常审核 / 执行），
  半路出现的「大家好 @全体成员」也不算广播。
  验收项 **J66**、真机复验 **R16** 已补进文档。

### 变更

- **`/testat all` 的 @全体候选扩到 8 条，并确认真机结论：机器人无法 @全体**。R1 抓到官方**入站**原文形态是
  `<@all>`（`@全体成员`）与 `@everyone`，而历史候选只穷举了带 `!` 的 `<@!all>` / `<@!everyone>`；
  补上 `<@all>`（卡片 + 纯文本两种通道）与纯文本 `@everyone` 后实测——**8 条全部提醒不到任何人**。
  因此 `mentionAll on`（活动「提醒@全体」）保持现状：只提示操作者手动 @ 一条，不假装能 @；
  真机清单 **R17** 标为已确认（否定），TODO 记为 `C9（记录）`。

### 新增

- **`@全体` 探测日志（真机清单 R1）**：群里出现「@全体 / @all / @everyone」类消息时，按 **info** 级记一条
  `mention probe: at-all candidate`——只记**形状**（前 16 个码点的转义预览、原始/清洗后长度、不可见码点、
  提及是否被剥离、清洗后是否是指令），不打印完整正文、不落库、不写审计；
  其它带 @ 或不可见字符的内容在 `LOG_LEVEL=debug` 下记 `mention probe: mention-like content`，
  其中 `stripped=false` 且 `isCommand=false` 表示这个提及格式没被 `stripBotMention` 识别出来。
  日志位置：`logs/qq-group-ops.log`，检索 `grep "mention probe"`。
  用途：回填 R1（官方事件里 @全体 的真实表示）→ 解锁 **B3（跳过 @全体）**。


## [0.15.0] - 2026-09-26

### 变更

- **卡片规范 v2 全面落地**（详见 [`docs/CARD-STANDARD.md`](docs/CARD-STANDARD.md)）：
  1. **按钮优先**：能一键完成的动作一律给按钮，卡片正文**禁止**写指令用法（用法统一进 `/help`）；
  2. **引导行全部废弃**：`CardSpec.buttonHint` 字段及 `cardify` / `cardifyAsync` / `cardFromText` 参数已删除，
     模板只渲染「标题 + 正文 + 按钮 + 页脚」；`cardify` 默认页脚「按钮不可用时可直接输入指令。」也一并清掉；
  3. **群内首行 @ 固定放标题下一行**（`<@!发起人>`），不再出现「有的在标题上、有的在标题下」；
  4. **列表类每页目标 5 条**：审计默认 10→5、报名名单 10→5、全局规则覆盖率总览 10→5；
     受键盘约束的按例外表（黑名单/处罚 4、关键词/待审批/活动 3、学院 4），例外原因写进规范；
  5. **列表口径统一** `共 N 条 · 第 X / Y 页`。
- **卡片文案去指令化**：`/bind`、`/perm`、`/whois`、`/alias`、`/notify`、`/rules` 卡片 footer 里的
  「详细用法：/help」类文案删除（卡上本来就有对应「…帮助」按钮）；
  审计 / 报名名单 / 覆盖率总览的「上一页、下一页：/xxx」删除（翻页本来就是回调按钮）。
- **「查看详情」改成按钮**：活动管理回执从 `活动配置：/activity info #短码` 改为「查看详情」回调按钮；
  报名未通过与发布回执此前已改完。
- **菜单去指令化**：常用 / 管理 / 审核 / 运营 / 活动 / 超管菜单只留标题、状态行与按钮，全部用法文字删除
  （超管菜单补「帮助」按钮指向 `/help perm`）。
- **通知卡只留业务信息**：处罚 / 申诉卡删掉「可直接在下面调整处罚」这类引导句，
  footer 只保留按钮做不到的「自定义禁言时长」；`withButtons=false` 时才补「解除禁言」指令。

### 备注

- **明确保留的等效指令**（规范 §9 逐条判定）：`/help` 体系内的用法文案、指令失败时的用法提示、
  无按钮通道（群广播 / 私信回执里的 `/activity join|quit`）、按钮无法表达的「自定义禁言时长」。
- **已知降级损失**：自定义按钮是官方内邀能力；纯文本降级时**指令按钮**的等价指令由模板自动列出，
  **回调按钮**没有可复制指令（老客户端只能改用 `/help` 查指令）。
- 验收项 **J62–J65** 已补进 [`docs/ACCEPTANCE.md`](docs/ACCEPTANCE.md)（菜单无指令文本 / 卡片无冗余指令行 /
  资料空态可直接复制的组合指令 / 按钮覆盖全部可执行动作）。


## [0.14.2] - 2026-09-26

### 变更

- **`/profile`、`/myperm` 与 `/whois` 统一口径**：群内**一切响应只走私信**（查询、设置确认、用法与错误提示、权限不足），
  群里静默；只有私信失败才回一条 `@发起人 + 请先私聊机器人再试`。
- **文案精简**：个人资料卡只留四行（姓名 / 学号（含年级）/ 班级 / 学院）+ 按钮
  （填姓名 / 填学号 / 填班级 / 填学院 / 活动 / 帮助）；我的权限卡只留两行
  （`权限等级：…` + `审批 ✓ · 规则 ✓ · 审核 ✗ · 导出 ✗`，超管多一项 `配置权限 ✓`）+ 按钮；用法说明只在输入有误时出现。

### 备注

- 数据本身不变，只是**落点**从群内改为私信；`/profile`、`/myperm` 卡片上的按钮改为指令按钮，避免回调把静默占位卡发回群里。

## [0.14.1] - 2026-09-26

### 修复

- **群卡片丢失按钮（真机回归）**：沙箱 / 未开通接口下给新用户发私信失败（400）时，按钮可用性被**全局**置为不可用，
  之后所有群卡片都降级为纯 Markdown（`keyboard:false`）。改为按 **user / group 分别记录**，
  并且「沙箱环境不能访问此资源」「应用无接口访问权限」类错误不再判定为「平台不支持自定义按钮」。
- **群内 @ 位置不统一**：@ 发起人此前插在卡片最顶部（标题上面），与关键词命中卡（标题下面）不一致；
  现在统一放在**标题下一行**。
- **卡片重复罗列指令**：按钮已经覆盖的动作（解除处罚 / 预设禁言时长 / 踢出 / 拉黑）不再在卡片里重复写指令，
  只保留按钮做不到的「自定义禁言时长」。

### 备注

- **沙箱机器人**的 C2C 主动消息对未互动 / 新用户返回 `400 沙箱环境不能访问此资源`：处罚通知、申诉推送、
  `/whois` 私信、`/export` 私信在沙箱下可能失败（正式上线后消失）；失败时按既有逻辑降级或回执。
- **官方踢人 / 群拉黑接口未开通**（真机返回 `400 应用无接口访问权限`）：`/blacklist` 的**入群审批拦截**仍然有效，
  但「已在群内踢出」与处罚卡上的「踢出 / 拉黑」官方动作会走失败回执。
## [0.14.0] - 2026-09-26

### 新增

- **消息侧正则规则 + 用户白名单**（§B1）：`/rules set regexRules <正则、正则>`、`/rules add|del regex <正则|序号>`，
  正则命中与关键词**同一条处罚管道**（按本群「命中处罚」执行，多个命中取最高动作）；
  `/rules add|del whitelist <QQ号|#短码|userId>` 维护的用户白名单与「审核员及以上豁免」叠加，
  命中白名单不警告、不撤回、不处罚、不写审计；违规处理子卡新增「正则白名单」子卡（列表 / 加 / 清空 / 恢复继承）。
- **审核日志 CSV 导出**（§B6）：`/export audit [数量]`（默认 10、最多 50；私信可带群号），
  CSV 只私信给操作者本人、群里静默；actor / target 统一脱敏（只保留首字符，不导出 openid 原文），
  导出动作本身写审计（`export_audit_records`）。

### 备注

- 正则配置存 `group_settings` 键值表（老库无需迁移）；单条 ≤200 字、列表 ≤50 条，写入前校验合法性，非法正则不落库。
- 多条正则用**顿号**分隔（避免拆坏 `a{1,3}` 这类含逗号的正则）；含顿号的正则请用 `/rules add regex` 逐条添加。
- 导出脱敏保留首字符，仅用于人工排查，不用于对外分发。

## [0.13.0] - 2026-09-26

### 新增

- **默认入口菜单改为「常用菜单」**（§F2）：`/menu` 无参数、群里空 `@机器人`、私信首次交互、
  未知指令回复一律出常用菜单——**帮助 / 我的资料 / 我的权限 / 活动 / 申诉**
  （未绑定 QQ号时显示「绑定账号」）。
- **群内回复统一在首行 @ 发起人**（§F1）：操作确认、菜单、反馈、错误提示，无论卡片还是回调回复，
  群内首行都是 `<@!发起人>`（真机验证：卡片 markdown 里的提及有效）；私聊不加。

### 变更

- **管理 / 超管入口只在私信暴露**（§F2）：群里不再出现「管理菜单 / 超管菜单」按钮，
  管理员要自己发 `/menu 管理`（权限校验不变，越权仍明确提示权限不足）；
  **超管菜单只能在私信使用**，群里发 `/menu 超管` 只提示去私信；
  私信默认菜单对审核员及以上显示「管理菜单」、对全局超管显示「超管菜单」。
- `sys`（`/menu 系统`）层级与常用菜单合并为同一张卡，只为兼容已发出的旧卡片按钮保留。

### 备注

- **纯文本通道不支持提及**：真机结论是官方只有 Markdown 卡片正文里的 `<@!openid>` 生效，
  纯文本 `content` 里的 `<@!openid>` 不生效；因此群内回复统一走卡片，卡片降级为纯文本时不含 @
  （平台能力限制，不是遗漏）。
- **@ 的豁免**：`test` 模块（`/test`、`/testmenu`、`/testat` 及对应回调）按现状不加；
  `silent: true` 的静默回复本来就不发消息；已经带 @ 的卡片（如 `/whois` 私信失败提示、
  关键词命中卡）不会被重复添加。

## [0.12.0] - 2026-09-26

### 新增

- **黑名单（本群 / 全局）**：`/blacklist` 卡片式管理（列表 / 添加 / 解除，列表每页 4 条、解除带二次确认）。
  - 本群黑名单需要审核员或以上；全局黑名单仅全局超级管理员，影响机器人**所有已绑定群**；
  - **入群审批最高优先级拦截**：命中黑名单的申请直接不通过，先于开关判断、自动通过、名单筛选与班级姓名等一切规则；
  - 加入黑名单时会先踢出（全局黑名单踢出所有绑定群）；本群黑名单同时调用官方群拉黑接口
    （接口失败不影响本地拦截）。
- **处罚事件推送与卡片上直接改处罚**：关键词命中并实际处罚（撤回 / 禁言 / 踢出 / 拉黑）后，
  私信推送给订阅者，卡片上可直接：
  - **解除处罚**：按记录逐项撤销（解除禁言、解除黑名单；已撤回的消息与已移出的成员无法恢复）；
  - **修改禁言时长**：预设 10分钟 / 1小时 / 1天 / 7天，也可用 `/punish mute <短码> <秒>` 自定义；
  - **踢出**、**拉黑本群**（带二次确认）、**拉黑全局**（只出现在全局超管的卡片上）。
- **处罚记录**：每条处罚生成 6 位随机短码（如 `#A1B2C3`），`/punish <短码>` 查看详情、
  `/punish list [+页码]` 查看本群最近记录；记录只保存规则说明与动作，**不保存消息原文**。
- **申诉能力**：当事人在群内发 `/appeal <#处罚短码> [理由]`，或点群内警告卡片上的
  「我要申诉」按钮（只有当事人能点，点击后机器人私信引导卡）。申诉推送给同一批订阅者，
  审核员可在卡片上**通过申诉**（= 解除处罚）/ **驳回申诉**，或直接改时长 / 踢出 / 拉黑
  （会记为「已调整处罚」）；群内对申诉完全静默。`/appeal list` 查看自己提交过的申诉。
- **`/notify punish` 独立订阅开关**：处罚通知与入群申请推送**相互独立**，可选择本群 / 全部群；
  订阅者需是该群的审核员或以上；`/notify punish test` 可自检私信通道。

### 变更

- `/notify` 帮助与菜单补充处罚通知入口；`/menu 审核` 新增「处罚记录 / 黑名单」，
  `/menu 超管` 新增「处罚推送 / 全局黑名单」。
- 数据保留策略一并清理**超期的处罚记录**与**已处理的申诉记录**（待处理申诉不自动清理），
  与审计记录同一保留期。

### 备注

- 处罚记录、申诉记录与黑名单条目都以**短码 / 展示名**对外，卡片与指令不暴露内部 openid；
  所有增删与调整动作都会写入审计。
- 处罚通知卡片里的「拉黑全局」只对全局超管显示；审核员只能拉黑本群。
- 官方群拉黑接口要求目标不在群中，因此加入本群黑名单的顺序是**先移出群、再拉黑**；
  官方接口失败时本地仍会拦截入群申请。

## [0.11.0] - 2026-09-25

### 新增

- **活动定时提醒**：`/activity set <短码> remindAt MM-DD HH:mm`（`clear` 取消）设置提醒时间，
  到点在**所有绑定群**各广播一次提醒卡；每个群只发一次、重启不重发
  （`ACTIVITY_REMIND_INTERVAL_MS` 控制扫描间隔，默认 1 分钟）。活动已取消 / 已关闭 / 已过报名截止时
  只清提醒、不广播；配置卡显示当前提醒时间。
- **命中关键词的完整反馈卡片**：命中后在群里 @ 当事人，并给出「命中规则 / **实际执行**的处理动作
  （含禁言时长）/ 群规则文案」，与审计记录保持一致。

### 变更

- **`/whois`、`/whois profile` 的查询结果一律走私信**：包括**未找到映射**、用法提示与**权限不足**——
  群里只在私信发送失败时回一条 @ 发起人的「请先私聊机器人再试」提示。理由：查询失败本身也会泄露
  「某个 QQ号 / 短码 / group_openid 是否存在」。
- **满员广播覆盖「事后调小名额」**：管理员把名额调小到等于已报名数（含冻结名额）时，也会往所有绑定群
  各发一次「活动已满」卡；仍复用去重表，每个群只发一次，名额大于已报名数时不广播。

### 备注

- 活动通知新增**令牌桶平滑**（`ACTIVITY_NOTIFY_RATE_PER_SECOND`，默认 5 条/秒，`0` = 关闭）：
  桶空时排队等待下一个令牌而不是丢通知；只作用于活动通知，入群申请推送不受影响。

## [0.10.0] - 2026-09-25

### 新增

- **学院 / 年级白黑名单真正参与入群审核**：`/rules` 的「名单筛选」不再只是保存展示——命中学院黑名单、
  或学院 / 年级不在白名单内会**直接拒绝**，不会被「自动通过」这类决策模式绕过；纯人工模式仍交人工，
  只在审核意见里标明原因。只配置名单也算「已配置入群审核规则」；年级 `2022` 与 `22` 两种写法等价；
  班级库缺失或答案里识别不到班级时，白名单判不通过、仅黑名单不拦并给出配置提示。
- **`/rules set rawMessageRetentionDays <天数>`**：原始消息保留天数可配置（`0` = 不保留，`clear` 归零），
  「更多设置」子卡新增「消息保留」按钮并显示当前值，也纳入「恢复本页继承」范围。
- **关键词子卡支持指令翻页**：新增 `/rules keyword [+页码]`（私信 `/rules keyword <群号|#短码> +页码`），
  卡片页脚给出可复制的「下一页 / 上一页」指令。

### 变更

- **`/whois` 与 `/whois profile` 群内完全静默**：群里发这两个指令时，只要结果已私信成功，群里不再回
  「已私信发送」提示；私信失败时仍会在群里回**唯一一条**不含结果的提示（请先私聊机器人再试）。

## [0.9.1] - 2026-09-25

### 备注

- 本版本为**纯内部重构**：拆分过大的源码与测试文件、收敛重复的投递逻辑、重写文档结构。
  不改变任何指令文案、按钮、回调数据、权限与行为；全量测试保持通过，因此没有用户可见的新增或修复条目。

## [0.9.0] - 2026-09-25

### 新增

- **规则菜单改版**：`/rules` 现在是「概览卡 + 子卡」结构（开关设置 / 入群审核 / 违规处理 / 关键词 / 名单筛选 / 更多设置），
  概览卡直接写明本群覆盖了哪些规则、其余继承全局默认；每张子卡都能就地修改，不用再记字段名。
- **开关一眼看懂**：所有开关键显示**当前状态**（如「过滤 开」），点一下即切换并回到同一张子卡；枚举项标出当前值。
- **一键恢复继承**：每张子卡可「恢复本页继承」，概览卡可「恢复全部继承」，被本群覆盖的规则会回落到全局默认。
- **关键词可以逐条管理**：`/rules add keyword <词>` 与 `/rules del keyword <词>`，关键词子卡分页展示、每条可单独删除。
- **学院 / 年级名单可以直接点选**：从班级库与年级列表里点选，支持白名单 / 黑名单切换。
- **全局规则与覆盖率总览**：`/rules all` 与群规则同样的操作方式；`/rules overrides` 可查看哪些群覆盖了哪些规则。

### 变更

- 帮助主题 `/help rules` 重写，覆盖新的卡片操作、关键词增删与恢复继承。

### 备注

- 学院 / 年级名单目前只用于保存与展示，**尚未参与入群审核判定**。
- 受卡片排版限制：关键词与学院列表分页只在卡片上翻页（学院每页 4 个），没有可复制的翻页指令。
- 原始消息保留天数暂时没有提供修改入口。

## [0.8.0] - 2026-09-25

### 新增

- **活动配置卡**：`/activity create <标题>` 直接给出配置卡——名额、学院 / 年级限制、报名截止时间、
  候补递补方式、报名通知开关、预览、开放报名、取消活动，全部点按钮完成。
- **报名改用点击操作**：活动卡片上的「我要报名 / 取消报名 / 活动详情 / 报名名单」都是点击即生效，并带二次确认。
- **报名结果只私信通知**：群里点击或手输 `/activity join|quit` 都**不会在群里刷屏**，成功、进入候补、失败原因
  一律私信本人（私信里可看姓名、学号、序号、当前人数）。
- **满员会自动公告**：名额刚满时，机器人在**所有绑定了该活动的群**推送「活动已满」提示，并说明后续报名将自动进入候补队列。
- **候补与名额释放**：名额满后自动进入候补；递补方式可在活动里选择「自动递补」或「管理员手动释放名额」（**默认手动**）；
  手动释放时优先把候补第一位转为正式报名，没有候补就把名额放回公开池。
- **报名截止时间**：到点后自动停止报名（无需人工关闭）。
- **活动订阅与提醒**：可按群订阅新活动；活动发布、变更、取消、递补成功都会私信相关的人，并设有每人每日提醒上限。
- **活动统计与导出**：可生成活动统计图（报名 / 候补 / 待释放名额与学院、年级分布），名单可导出为 CSV 私信给管理员。
- **活动可绑定多个群**：`/activity bind|unbind <#活动短码> <群号|#群短码>`，发布与满员公告都会覆盖所有绑定群。

### 变更

- 活动列表按状态分组，列表与卡片上的操作按钮都是点击即生效；名单默认脱敏（只显示姓名与班级），
  需要学号 / 学院时再点「完整信息」。
- **群内通知去噪**：报名相关消息不再进群，只有活动卡片、满员公告这类对全群有意义的消息才会出现在群里。

### 备注

- **`@全体成员` 官方不支持**：机器人无法 @ 全群（多种写法实测均无效）。开启活动提醒时，会给操作者私信提示
  「如需通知全群请手动 @ 一条」，并可用订阅私信作为替代触达。
- 统计图需要额外安装图像组件；未安装时自动降级为文字统计，不影响使用。
- 名单过长时不再截断发送，而是提示改用导出方式获取完整名单。
- 满员公告只在「本次报名恰好填满名额」时触发；管理员事后调小名额不会补发。
- 活动提醒为「同一活动同一人去重 + 每人每日上限」，没有更细的发送频率限制。

## [0.7.0] - 2026-09-25

### 新增

- **`@` 自检指令 `/testat`**（仅全局超管，在群里使用）：机器人会连发多条不同写法的测试消息，
  用真机确认「群里 @ 到人」到底哪种方式有效；`/testat all` 可额外测试 @全体候选写法（会打扰全群，需二次确认）。

### 备注

- 实测结论：**卡片内的 @ 有效**，纯文本 @ 与 `@全体成员` 都无效。项目内所有 @ 反馈因此统一使用卡片内 @。

## [0.6.1] - 2026-09-25

### 变更

- **`/whois` 结果只走私信**：群里发指令时，结果只私信给操作人，群里最多回一条「已私信发送」提示；
  私信发不出去时也只提示「先私聊机器人再试」，不会把结果降级到群里。群里可以直接 @ 某位成员来查询；
  @ 昵称无法反查时会给出提示。
- **短码只用数字与大写字母**（更容易口头报、手抄）；历史遗留的小写短码会在启动时自动更换，
  旧短码随即失效，需要重新获取；手输时不区分大小写。
- **个人资料年级统一两位**：`/profile set year` 只接受 `22`、`23` 这类两位写法，填四位年份会被拒绝；
  班级库里的四位年份会自动转成两位。

### 备注

- 文档与示例中不再包含真实账号标识，并加入自动检查，防止类似的隐私信息再次被写进仓库。

## [0.6.0] - 2026-09-24

### 新增

- **`/profile set` 支持一条消息填完**：`/profile set 材化2211 张三 22123456789`，顺序随意、分隔符随意
  （空格、`-`、`+`、`/`、`,`、`、`、`|`、`;`、中英文括号都可以），甚至完全不带分隔符也能识别；
  也支持 `字段=值` 显式写法消歧。**识别不确定时不会写入**，而是列出识别结果并提示改用 `字段=值`。
- **`/whois profile <QQ号|userId|#用户短码>`**（仅全局超管）：一次看到某人的 userId、QQ号、短码与个人资料。
- **班级数据加工**：`pnpm class:index` 把教务导出的班级数据加工成便于后续使用的两份产物
  （JSON 与 SQLite），并补齐学院、专业、班级之间的对照关系。
- **班级 / 学院 / 专业别名表**：全局超管可用 `/alias` 维护「习惯写法 → 规范名」的映射
  （例如 `环工2214` → `环境类2214`），别名同时用于个人资料识别与入群审核匹配；匹配忽略空格、长别名优先。

## [0.5.0] - 2026-09-23

### 新增

- **入群申请自动收敛**：超过有效期（默认 7 天）或官方已不再返回的待审批申请会自动标记为过期，
  不再出现在待审批列表里，但审计与 `/whois` 仍可追溯。
- **`/whois` 更好用**：查申请短码时会给出完整申请详情（群、申请人、理由、状态、申请与处理时间、处理人）；
  不带参数时直接查当前群（群聊）或你自己（私聊）。

### 修复

- 修复 `@机器人` 发指令收不到回复（提及前缀与不可见字符的剥离）。
- 修复回调翻页的两个真机问题：不再尝试用互动回执 id 当被动回复（官方会拒绝），
  以及被动回复失败后不再永久丢失按钮。
- 修复入群申请「回答」显示为空（兼容两种官方提问方式）。
- 修复全局超管在私信里查看群指令帮助时被误判为权限不足。

## [0.4.0] - 2026-09-23

### 新增

- **管理类指令卡片化**：`/audit`（分页）、`/test`（自检）、`/sync`（一键同步）、`/approve`、`/reject`、
  `/notify`（订阅开关点击即生效）都改为卡片。
- **活动列表分页**，菜单里可直接翻页。
- **菜单覆盖全部指令**：系统 / 管理 / 超管 / 活动 / 审核操作 / 活动运营子菜单，导航点击即出下一张卡。
- **所有指令输出统一为卡片**：包括用法提示、权限提示与错误提示。

### 变更

- 统一按钮排版：一行按钮文字合计不超过 12 字、单个按钮不超过 10 字、整盘不超过 5 行。
- 回调操作的反馈会 @ 操作人；卡片不再堆砌手动指令（统一在 `/help` 里查）。
- `/notify test` 在没有可审批的群时也能自检推送通道。

## [0.3.0] - 2026-09-23

### 新增

- **卡片标准**：所有指令输出统一为卡片；导航 / 查看 / 翻页 / 开关用点击操作，需要参数或不可逆的动作用指令按钮；
  列表分页同时给出可复制的 `+页码` 写法作为降级。
- **首批样板指令卡片化**：`/help`、`/status`、`/pending`、`/rules`。
- **`/testmenu`**（仅全局超管）：回调按钮翻页试验，用于验证官方互动事件链路。

## [0.2.0] - 2026-09-23

### 新增

- **QQ 端交互菜单 `/menu`**：三级菜单（系统 / 管理 / 超管），入口按权限显示；空 `@机器人`、
  未知指令、首次私信都会引导到菜单。
- **统一卡片模板**：菜单、入群申请、活动卡片共用同一套布局，并统一做三级降级
  （Markdown + 按钮 → Markdown → 纯文本）。
- **指令帮助**：`/help <指令>` 查看详细用法，支持中文别名，并按权限过滤。
- **本群超级管理员**：新增 `/perm grant gsuper`，只在本群内等价于超级管理员，拿不到平台级能力。
- **展示不再暴露内部标识**：未绑定的用户 / 群改用随机短码展示，`/whois` 是唯一能还原真实系统 id 的指令（超管限定）。
- **入群审核规则引擎**：5 档决策模式（人工 / 全自动 / 命中通过 / 命中拒绝 / 未命中拒绝），
  可要求回答包含班级、姓名或匹配自定义正则，并可展示审核意见。
- **班级库导入**：`pnpm class:index` 生成班级索引，供入群审核与个人资料使用。
- **关键词处罚动作**：命中关键词后可撤回、禁言、移出、移出并拉黑。
- **入群申请推送 `/notify`**：订阅后在私聊收到待审批卡片，可直接点「同意 / 拒绝」并二次确认。
- **个人资料 `/profile`**：维护班级 / 学院 / 姓名 / 学号，班级会自动带出学院。
- **活动发布 / 报名 / 管理**：创建活动、配置名额与链接、按学院和年级限制报名、开放报名与查看名单。
- **审核员豁免**：审核员及以上的消息不再做关键词判定。

### 变更

- 官方调用域名统一迁移。
- 全局规则可查看与修改，未单独配置的群自动继承全局默认。
- 用户可见输出只显示 QQ号 / 群号 / 短码，不再暴露内部标识。

## [0.1.0] - 2026-09-23

首个可运行版本：基于 QQ 官方开放平台 API 的群管理与运营机器人，覆盖群管、入群与内容审核、活动报名、信息导出与审计日志。

### 新增

- **官方接入**：官方 REST 客户端、WebSocket 网关（鉴权、心跳、事件分发）、自动重连与断线恢复、
  限流识别与退避、出站消息节流与被动回复配额保护、凭证与网关地址缓存。
- **入群审批**：待审批列表、从官方同步申请、同意 / 拒绝，自动通过可选；审批以官方接口成功为准。
- **内容审核**：群关键词驱动审核，命中后按群配置发送警告并记录审计。
- **群规则**：按群查看与修改关键词、警告文案、禁言时长、关键词过滤、入群审核、自动通过、导出与启用开关。
- **官方管理动作**：禁言与移出群成员。
- **审计查询**：按群查看操作审计。
- **权限管理**：超级管理员、群管理员、审核员三级，支持自助查看权限与动态帮助。
- **身份映射**：OpenID ↔ QQ号 / 群号的手工绑定；除帮助与绑定外，使用指令前需要先绑定。
- **私信指令**：群管理指令可在私信中通过群号指定群执行。
- **数据持久化**：默认零配置 SQLite，可选 PostgreSQL；全部状态重启不丢；内置过期数据清理。
- **可观测性**：结构化日志（控制台 + 文件），统一调用与耗时记录，日志不含敏感信息。
- **交付形态**：Dockerfile 与 Docker Compose，附带架构、配置、路线图、合规、决策记录与验收清单等文档。

[Unreleased]: https://github.com/muyangplus/qq-group-ops/compare/v0.14.2...HEAD
[0.15.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.14.2...v0.15.0
[0.14.2]: https://github.com/muyangplus/qq-group-ops/compare/v0.14.1...v0.14.2
[0.14.1]: https://github.com/muyangplus/qq-group-ops/compare/v0.14.0...v0.14.1
[0.14.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.13.0...v0.14.0
[0.13.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.12.0...v0.13.0
[0.12.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.11.0...v0.12.0
[0.11.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.10.0...v0.11.0
[0.10.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.9.1...v0.10.0
[0.9.1]: https://github.com/muyangplus/qq-group-ops/compare/v0.9.0...v0.9.1
[0.9.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.8.0...v0.9.0
[0.8.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.7.0...v0.8.0
[0.7.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.6.1...v0.7.0
[0.6.1]: https://github.com/muyangplus/qq-group-ops/compare/v0.6.0...v0.6.1
[0.6.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.5.0...v0.6.0
[0.5.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/muyangplus/qq-group-ops/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/muyangplus/qq-group-ops/releases/tag/v0.1.0
