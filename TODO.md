# TODO

项目当前**大版本计划**、**已知限制**、**待澄清问题**、**待真机验收**与**新功能待办**的唯一入口。
已完成的历史变更见 [CHANGELOG.md](./CHANGELOG.md)；阶段目标与退出条件见 [docs/ROADMAP.md](./docs/ROADMAP.md)。

标记约定：`[x]` 已完成 / `[ ]` 待办；标注「（真机）」的项需要真实 QQ 群环境才能验收。

## 大版本计划（Roadmap 分解）

> 把 [docs/ROADMAP.md](./docs/ROADMAP.md) 的 Phase 1–4 范围与退出条件逐项拆成可勾选清单，
> 大件（Web 后台 / 内容安全 / AI）在这里跟踪进度：做到哪一项勾哪一项。

### Phase 1：MVP 核心（收尾）

**范围**

- [x] 配置加载与校验（含 `.env.example` 同步）
- [x] `QQOfficialClient` 官方 REST 封装：token 与网关地址缓存、401 自动刷新、出站节流与 22009 重试、被动回复配额拦截
- [x] 官方 **WebSocket** 网关：自动重连 + Resume 会话恢复 + 心跳 ACK 超时检测 + 指数退避 + 限流冷却
- [ ] 官方 **Webhook** 模式：当前代码里没有任何 webhook 实现；如需支持，要新增 HTTP 接入 + 签名校验，并与网关二选一装配
- [x] 入群申请审核：指令 / 卡片通过拒绝、官方审批接口、自动通过、`/sync` 官方同步
- [x] 规则化入群审核：班级库 + 班级/姓名/答案正则 + 5 档决策模式 + 审核意见
- [x] 消息规则：关键词命中动作（撤回 / 警告 / 禁言 / 移出 / 拉黑）+ 审核员及以上豁免
- [ ] 消息规则的**正则与白名单**维度：当前消息侧只有关键词；正则目前只用于「入群答案匹配」
- [x] 入群申请推送 `/notify`：订阅粒度到群、Markdown 卡片、快捷同意/拒绝、投递去重
- [x] 管理员命令族：`/rules`、`/audit`、`/sync`、`/pending`、`/approve`、`/reject`、`/perm`、`/status`、`/test`、`/whois`、`/profile`、`/alias`、`/activity` 等
- [x] OpenID ↔ QQ号/群号 绑定与全部状态持久化（SQLite 默认 / PostgreSQL 可选）+ 过期数据清理
- [x] 多群配置：统一默认 + 单群覆盖（字段级继承、恢复与覆盖率总览）
- [x] Dockerfile / docker-compose.yml（含 `pnpm db:up` profile）
- [x] 单元与集成测试（87 文件 / 691 用例）与 GitHub Actions CI

**退出条件**

- [ ] 测试群内跑通一次完整入群审批闭环（真机）
- [ ] 关键词命中后按规则执行动作并记录日志（真机）
- [ ] 审计日志可持久化、可查询（真机抽查）
- [x] `typecheck` 与 `test` 通过（本仓以等价命令验证：`tsc -p tsconfig.json --noEmit` 干净、691 用例通过）
- [ ] `pnpm build` 通过：等价命令 `tsc -p tsconfig.json`（输出 `dist/`），**尚未在本仓验证过**
- [ ] Docker Compose 能启动 bot 与数据库
- [ ] README 快速开始步骤可复现（对照 `docs/OPERATIONS.md`）

### Phase 2：Web 后台、内容安全与活动运营

- [ ] 管理 API（Fastify / Node.js）+ 登录鉴权 + 限流 + 最小权限
- [ ] Vue 3 + TypeScript 管理后台
- [ ] 后台功能：审核队列、规则配置、日志查询、权限管理
- [ ] 文本内容安全 API 接入（含误报 / 漏报处理策略）
- [ ] 图片 / 文件 / 链接审核（以官方能力为准）
- [ ] 好友申请 / 群邀请审核（先真机验证官方能力，再决定是否实现）
- [x] 活动报名：活动创建、报名收集、名单管理（0.8.0）
- [ ] 活动**签到统计**
- [ ] 信息导出补齐：活动 CSV 导出已有（0.8.0），**审核日志导出**与**统一脱敏策略**待做
- [ ] 退出条件：公网后台具备登录鉴权 / 限流 / 最小权限；后台能完成审批与规则配置；内容安全有误报漏报策略；导出有权限校验与脱敏

### Phase 3：AI 与运营

- [ ] AI 判断入群理由（只把可疑内容送入模型，控制成本）
- [ ] AI 辅助疑难内容审核
- [ ] 举报与申诉流程 —— 细化项见「新功能待办 · 申诉能力」（含一项待澄清）
- [ ] 统计报表与自动化策略
- [ ] 退出条件：AI 仅作辅助且保留人工复核入口；有误判申诉与回滚机制；模型成本在可接受范围

### Phase 4：安全加固与开源发布

- [ ] 安全审计与依赖更新策略（`pnpm audit` 周期化、依赖升级与回归流程）
- [ ] 数据合规：**个人数据删除**能力（过期数据清理已有；按用户删除 / 导出个人数据未做）
- [x] 完整文档、示例配置与部署指南（R6：README ≤250 行 + `COMMANDS` / `OPERATIONS` / `DEVELOPMENT` / `CONFIGURATION` / `DECISIONS` / `ARCHITECTURE`）
- [x] 版本发布流程与 CHANGELOG（Keep a Changelog + SemVer，已发布 0.9.1 并推送标签）
- [x] 贡献者指南与 Issue/PR 模板（`CONTRIBUTING.md` + `.github/ISSUE_TEMPLATE/*` + `PULL_REQUEST_TEMPLATE.md` + CI）
- [ ] 退出条件：安全与合规检查清单完成；至少一次完整部署演练与备份恢复演练；发布首个可用的 GitHub Release（**标签已推，Release 页面尚未创建**）

## 待澄清

- [ ] **申诉「自动解除处罚」里的「拉回」具体指什么？** 撤回消息 / 拉黑 / 移出群？立项前必须确认（见「新功能待办 · 申诉」）。

## 已知限制

- [ ] **活动统计图片是可选能力**：`@napi-rs/canvas` 是原生依赖，**未随包安装**时「统计图片」按钮不生成、
  回调降级为文字统计卡（设计如此，不影响启动与报名）。启用方法：`pnpm add @napi-rs/canvas`。
  字体策略：系统字体优先（Windows 雅黑 / Linux Noto CJK / 文泉驿 / 苹方），缺失时从
  `ACTIVITY_STATS_FONT_URL` 下载并缓存到 `data/fonts/`（gitignored，不随包提交）。
- [ ] **规则的学院 / 年级名单尚未参与入群审核**：`allowColleges` / `denyColleges` / `allowYears` /
  `denyYears` 已支持字段级覆盖、继承、点选与持久化，但 `JoinRuleEvaluator` 还没读它们做白黑名单判定
  （`/help rules` 已注明「当前仅存储展示」）。接入点：评估器读 `configStore.get(groupId)`。
- [ ] **关键词子卡分页没有文本降级**：只有「上一页 / 下一页」按钮，没有可复制的 `+页码` 指令。
- [ ] **规则学院点选每页 4 个**：受卡片排版硬约束（每行按钮文字 ≤12 字、整盘 ≤5 行），
  不是理想的 5 个/页。
- [ ] **`/rules set rawMessageRetentionDays` 没有入口**：该字段在持久化清单里，但 `/rules set` 用法中未暴露。
- [ ] **全局规则卡「种子默认」的覆盖展示存在重启前后边界差异**：纯展示问题，不影响实际生效值。
- [ ] **活动满员广播只在「本次报名恰好填满」时触发**：管理员事后把名额调小到等于已报名数，不会补发已满卡。
- [ ] **活动通知没有更细的节流**：只有「同活动同人去重 + 每人每日封顶」，更细的 qps 级限速沿用既有
  `SendThrottle`，没有针对活动通知单独做令牌桶。
- [ ] **`@全体成员` 官方能力不可用**：真机穷举 5 种写法（markdown `@everyone` / `<@!all>` / `<@!everyone>` /
  纯文字 / 纯文本 `<@!all>`）全部失效；当前方案是「发布回执提示管理员手动 @ 一条」+ 订阅私信兜底。
- [ ] **`src/services/retention.ts` 行尾是 CRLF（内部整洁）**：与 `.gitattributes` 的
  `* text=auto eol=lf` 不一致（该文件是历史提交留下的，仓库 blob 即为 CRLF）。修法：单独一次
  `chore:` 提交把它统一成 LF（纯空白改动，`tsc` + 全量测试即可验证）。

## 待真机验收

- [ ] `docs/ACCEPTANCE.md` 的 **J32–J56**（活动重构 / 规则菜单重构）与 **M 组** 尚未在真机跑过。
- [ ] 真机确认：群内点击报名/取消后**群里完全静默**、结果只走私信；私信失败时只出现不含结果的提示。
- [ ] 真机确认：报名满员后**所有绑定群各收到一次**「活动已满」卡；重复触发不重复发。
- [ ] 真机确认：规则开关点击后标签变为「当前状态」、`恢复本页继承 / 恢复全部继承` 生效。
- [ ] 真机确认：`/whois` 在群内仍会回「已私信发送」提示卡（见待办 1，计划改为完全静默）。

## 新功能待办

- [ ] **`/whois` 群内完全静默**：群里发 `/whois` 时连「已私信发送」提示也不回复，结果只走私信
  （当前群内会回一张提示卡）。注意保留「私信失败」时的唯一提示。
- [ ] **活动定时提醒**：管理员可给活动设置定时任务，到点后在**所有绑定群**发送活动报名提醒
  （建议做成活动级配置 + 复用 `RetentionService` 的周期任务骨架或独立轻量定时器；注意主动消息限额与去重）。
- [ ] **命中广告规则时发完整卡片并 @ 对方**：现在命中关键词只做撤回 / 警告 / 禁言等动作，
  没有「完整卡片 + @ 当事人」的反馈；需要一张包含命中规则、处理动作、申诉入口的卡片。
- [ ] **审核员及以上可订阅「审核事件」**：
  - 订阅后接收**对应群聊的撤回事件**推送，卡片携带**完整原消息**与相关人员信息；
  - 需要新的订阅维度（用户 + 群 + 事件类型），可复用 `notification_subscriptions` 的粒度设计；
  - 与 `notification_deliveries` 一样做投递去重，避免事件重投刷屏。
- [ ] **申诉能力**（对应 Phase 3「举报与申诉流程」）：
  - 申诉事件发送给**订阅了该事件**的人员；
  - **仅违规本人可发起申诉**（其他人发起一律拒绝并说明）；
  - 申诉卡片在**触发时私聊对方**，包含申诉按钮（**只可触发一次**）；
  - 卡片必须说明「**恶意申诉可能加重处罚**」；
  - 申诉通过后**自动解除处罚**：禁言 / 拉回（待澄清）/ 去除黑名单。
  - 依赖：违规处罚记录（audit）与处罚动作的可逆性；可能需要新增「处罚记录 + 申诉状态」表。
- [ ] **不响应「@全体成员」消息**：群里有人 @全体 时，机器人应直接跳过——不回复、不送关键词审核、
  不产生审计与处罚，避免被无关的全员消息打扰。
  - 待确认：官方群聊消息里「@全体」的具体表示（机器人**自己发** @全体 已确认不可用，但**别人发**的
    消息可能带 `mention_everyone` 字段或 `<@!all>` 之类的提及段）。落地前先真机抓一条样本再定实现。
  - 参考位置：事件映射/路由入口（群消息进 `MessageGuardService` 之前）做短路最省事。

## 已完成但值得回看的项

- [x] `/testat` @ 渲染自检与真机结论（0.7.0）
- [x] 仓库隐私守卫 `test/privacyGuard.test.ts`（0.6.1）
- [x] 活动模块重构：回调报名、群内静默、多群绑定、满员广播、候补与手动释放、截止时间、订阅推送、统计图片与 CSV（0.8.0）
- [x] 规则菜单重构：当前状态语义、字段级继承与恢复、关键词逐条增删、覆盖率总览（0.9.0）
- [x] 代码与文档整理（R1–R6）：门面/测试/适配层/服务拆分、统一 PushService、README + 六份文档重写（0.9.1）
